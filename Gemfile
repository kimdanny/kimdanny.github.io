source "https://rubygems.org"

# Matches GitHub Pages' own build exactly, so local output == deployed output.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.x (webrick left stdlib in 3.0).
gem "webrick", "~> 1.8"
